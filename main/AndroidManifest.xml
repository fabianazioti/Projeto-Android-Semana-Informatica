<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifsuldeminas.afinal">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".CardViewActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".AgendaDivActivity" />
        <activity android:name=".ApoioActivity" />
        <activity android:name=".RedesSociaisActivity" />
        <activity android:name=".EnventoActivity" />
        <activity android:name=".InformacoesActivity" />
        <activity android:name=".CadastroUsuActivity" />
        <activity android:name=".ListaCursoActivity" />
    </application>

</manifest>